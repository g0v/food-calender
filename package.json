{
  "name": "food-calender",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "deploy": "next build && next export && touch out/.nojekyll && git add out -f && git commit -m gh-pages && git push origin `git subtree split --prefix out master`:gh-pages --force && git reset HEAD^"
  },
  "dependencies": {
    "axios": "^0.19.2",
    "lodash": "^4.17.15",
    "next": "11.1.0",
    "react": "16.13.1",
    "react-dom": "16.13.1"
  }
}
